# v1.0.27 Release Design — Spec-Complete Literature Pipeline

Date: 2026-05-29

## Goal

Finalize the literature pipeline simplification (spec: `2026-05-29-literature-pipeline-simplification-design.md`)
by adding the two remaining spec-required tests, bumping to v1.0.27, committing all working-tree changes,
and publishing to GitHub and PyPI.

## What Is Already Done

All 7 `obsidian_pipeline_*` tools are implemented and the literature profile (17 tools) matches the spec exactly.
165 tests pass. READMEs, plugin manifests, and SKILL.md files have been updated in the working tree.

## Gaps to Close

### Missing tests (2)

**test_pipeline_migrate_layout_apply_updates_managed_paths_and_links**
- Set up old-layout files (literature note with old PDF path, old mineru path).
- Call `obsidian_pipeline_migrate_layout(dry_run=False)`.
- Assert: files physically moved; literature note YAML paths updated; old wikilinks replaced.

**test_pipeline_repeated_mineru_parse_overwrites_machine_assets_preserves_user_content**
- First ingest, then write user content into `## Reading Notes` and set `status: reading`.
- Mock MinerU; call `obsidian_pipeline_parse_with_mineru` again.
- Assert: `paper.md` overwritten (machine asset regenerated); `status: reading` and `## Reading Notes` preserved.

### Version bump

| File | Change |
|---|---|
| `pyproject.toml` | `1.0.26` → `1.0.27` |
| `.claude-plugin/plugin.json` | `1.0.26` → `1.0.27` |
| `.codex-plugin/plugin.json` | `1.0.26` → `1.0.27` |

## Commit Strategy

**Commit 1 — feat: literature pipeline simplification**
- All working-tree changes: 7 pipeline tools, profile system, docs, README, SKILL.md.

**Commit 2 — test+chore: add spec-required tests and bump to v1.0.27**
- Two new tests.
- Version bump in three files.

## Release

- **PyPI**: `python -m build` → `twine upload dist/*`
- **GitHub release**: `v1.0.27 — Literature Pipeline` via `gh release create`
  - Release notes: 17 default tools, workflow overview, profile switching, breaking-change note.

## Success Criteria

- All spec-required tests pass (including the two new ones).
- `get_registered_tool_names("literature")` returns exactly 17 tools.
- Real-world smoke test: single Zotero item ingest creates a stable literature note.
- Real-world smoke test: collection ingest batch-reports succeeded/failed counts.
- Package published to PyPI as `zotero-obsidian-mcp==1.0.27`.
- GitHub release `v1.0.27` created with release notes.
